// Initialize...
// a constant called paddleWidth set to 10
// a constant called paddleHeight set to 80
// a constant called paddleSpeed set to 2
// a variable called paddleLeftY set halfway down the canvas
// a variable called paddleRightY set halfway down the canvas

function setup() {
  createCanvas(400, 400);
  // Switch p5 to draw rectangles from their center
}

function draw() {
  background(0);
  // Adjust leftPaddleY variable
  // based on whether W or S keys are being held
  // using speed constant

  // Draw white rectangles for each of the paddles
  // using the corresponding variables and constants
  // the center of each paddle is 20 pixels from the edge
}
