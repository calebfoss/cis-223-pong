// Initialize...
// a constant called ballSize for ball square length of side set to 10
// a variable called ballPosX for ball's x position set to halfway across canvas
// a variable called ballPosY for ball's y position set to halfway down canvas
// a variable called ballSpeedX for ball's x speed set to 2
// a variable called ballSpeedY for ball's y speed set to 0

function setup() {
  createCanvas(400, 400);
  // Switch p5 to draw rectangles from their center
}

function draw() {
  background(0);
  // Draw a white square for the ball using the corresponding variables and constants
  // Adjust ball's position based on its speed
  // If ball is hitting the top or bottom of canvas
  // Reverse its vertical speed
}
